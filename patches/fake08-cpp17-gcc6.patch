diff --git a/source/cart.cpp b/source/cart.cpp
index bd5e71f..23b63dc 100644
--- a/source/cart.cpp
+++ b/source/cart.cpp
@@ -26,7 +26,9 @@
 
 
 static char const *legacyCompressionLut = "\n 0123456789abcdefghijklmnopqrstuvwxyz!#%(){}[]<>+=/*:;.,~_";
-static std::regex _includeRegex = std::regex("\\s*#include\\s+([\\\\/A-Za-z0-9_\\-\\.]+)");
+/* GCC 6.3 <regex> rejects escaped '\-'/'\.' inside brackets ("invalid range").
+ * Use a literal trailing '-' and unescaped '.' (both literal in a bracket). */
+static std::regex _includeRegex = std::regex("\\s*#include\\s+([\\\\/A-Za-z0-9_.-]+)");
 
 //https://github.com/samhocevar/zepto8/blob/b1a13516945c49e47495c739e6a43a241ad99291/src/pico8/code.cpp
 // Move to front structure
diff --git a/source/emojiconversion.cpp b/source/emojiconversion.cpp
index 759a568..fbb704a 100644
--- a/source/emojiconversion.cpp
+++ b/source/emojiconversion.cpp
@@ -23,7 +23,11 @@
 #include <codecvt>
 #include <cstring>
 #include <regex>
+#if defined(__cpp_lib_string_view)
 #include <string_view>
+#else
+#include <experimental/string_view>
+#endif
 #include <map>
 
 #include "emojiconversion.h"
diff --git a/source/emojiconversion.h b/source/emojiconversion.h
index 4ad3049..95cec41 100644
--- a/source/emojiconversion.h
+++ b/source/emojiconversion.h
@@ -1,6 +1,18 @@
 #include <string>
 #include <regex>
 
+/* GCC 6 has only partial C++17: no std::string_view. Fall back to the
+ * experimental header (same API) so this builds on the SF3000 toolchain. */
+#if defined(__cpp_lib_string_view)
+#include <string_view>
+#else
+#include <experimental/string_view>
+namespace std {
+    using string_view = experimental::string_view;
+    using u32string_view = experimental::u32string_view;
+}
+#endif
+
 struct charset
 {
     // Convert between UTF-8 strings and 8-bit PICO-8 strings
diff --git a/source/synth.cpp b/source/synth.cpp
index 9e5c8db..3596307 100644
--- a/source/synth.cpp
+++ b/source/synth.cpp
@@ -13,6 +13,11 @@
 #include "synth.h"
 #include "audioOptimizations.h"
 
+/* GCC 6 lacks C++17 inline static data members; define them out-of-line. */
+float z8::synth::lastadvance = 0.0f;
+float z8::synth::sample = 0.0f;
+float z8::synth::lsample = 0.0f;
+
 //#include <lol/noise> // lol::perlin_noise
 #include <cmath>     // std::fabs, std::fmod
 #include <algorithm> //std::min, std::max
diff --git a/source/synth.h b/source/synth.h
index f2c6027..7d0f101 100644
--- a/source/synth.h
+++ b/source/synth.h
@@ -41,9 +41,9 @@ public:
     //can be declared in synth.cpp
     //also not threadsafe, but this is all single threaded anyway
     private:
-        inline static float lastadvance;
-        inline static float sample;
-        inline static float lsample;
+        static float lastadvance;
+        static float sample;
+        static float lsample;
         
 };
 
